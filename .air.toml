root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/api.exe ."
entrypoint = "./tmp/api.exe"
delay = 1000
exclude_dir = ["tmp", "storage"]
include_ext = ["go", "env"]
kill_delay = 500
stop_on_error = true

[log]
time = true
